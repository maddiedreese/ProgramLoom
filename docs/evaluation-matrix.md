# Evaluation traceability matrix

This file is the delivery ledger for the 96-item ProgramLoom evaluator. A criterion advances from **planned** only when linked implementation, test, and production evidence exist. A polished screen alone is not evidence of a round trip, rule, scope boundary, or side effect.

| Area                  | Criterion IDs |  Count | Current state                                                                                      | Required evidence                                                                                                              |
| --------------------- | ------------: | -----: | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Call for papers       | CFP-01–CFP-16 |     16 | Deployed and production exercised; final supplemental evaluator completed S1/S2 before budget stop | Anonymous access, conditional fields, draft/resume/edit, proposal tracker, submission detail, and controlled delivery evidence |
| Abstract management   | ABS-01–ABS-14 |     14 | Deployed, tested, and production exercised                                                         | Two-round scoring, assignment/scoping, blind review, COI, aggregate/export, and AI evidence                                    |
| Speaker management    | SPK-01–SPK-16 |     16 | Deployed, tested, and production exercised                                                         | Roster/import, portal scoping, profiles, tasks, files, email/logistics, and handoff evidence                                   |
| Content management    | CNT-01–CNT-14 |     14 | Deployed, tested, and production exercised                                                         | Requests, R2 uploads, versions, comments, constraints, approval, history, and real ZIP evidence                                |
| Agenda and scheduling | AIA-01–AIA-08 |      8 | Deployed, tested, and production exercised                                                         | Configuration, placement persistence, two conflict classes, move/clear, publish, assisted scheduling, and calendar evidence    |
| Public widgets        | EMB-01–EMB-16 |     16 | Deployed, tested, and production exercised                                                         | Five anonymous widgets, search/filter/detail, navigation, itinerary persistence/ICS, embeds, and live propagation evidence     |
| Speaker CRM           | CRM-01–CRM-12 |     12 | Deployed, tested, and production exercised                                                         | Directory, filters, history, fields, import/dedupe, pipeline, segments, handoff, delivered outreach, and analytics evidence    |
| **Total**             |               | **96** |                                                                                                    |                                                                                                                                |

## Evidence states

- **Planned:** requirement understood; no completion claim.
- **Implemented:** general-purpose production code exists.
- **Tested:** the relevant behavior and negative/scoping cases pass repeatably.
- **Production verified:** the deployed application was exercised with recorded URL, identity, timestamp, and result.
- **Complete:** all evidence above exists, including any human-only mailbox, calendar, file, or multi-browser protocol.

The release evaluator validates all 96 IDs and 20 ordered scenarios. Final evidence collection must follow CFP → abstract management → speaker management → content → agenda → embeds, preserving generated state between areas. CRM then runs as required extra scope.

## CFP implementation evidence

- Event access resolves organization owners/admins and scoped event roles server-side.
- Organizer APIs support form create/read/update/delete, protected deletion, publish/unpublish, field create/update/delete, tracks, and conditional rules.
- Select options and conditional values round-trip as structured JSON; forms require a field and a valid future deadline, when configured, before publication.
- The organizer UI exposes field types, required flags, sections, options, deadlines, draft policy, submission limits, confirmation messaging, and conditional show/hide/require rules.
- Local Worker + D1 runtime protocol verified authenticated create → fields → conditional rule → publish → read-back on 2026-08-09. This is development evidence, not production-complete evaluator evidence.
- Anonymous submitter flow renders published configuration, applies conditional visibility/requirements on both client and server, supports hashed private draft/edit tokens, enforces availability/edit windows and submission limits, transitions drafts into the review queue, and records real Resend confirmation attempts. Local draft → validation rejection → submit → private read-back passed on 2026-08-09; the final production run additionally preserved the public submission confirmation, private proposal tracker, provider state, and controlled-inbox receipt.
- Organizer submission management now provides owner/admin-scoped search and status filters, structured answer/submitter detail, aggregate review progress, decision queues, and auditable status transitions. Local filtered list → detail → accept queue → audit read-back protocol passed on 2026-08-09.

## Abstract review implementation evidence

- Organizer review configuration supports ordered multi-round evaluation, blind/non-blind mode, weighted numeric criteria, scored-select criteria, reviewer notes, required fields, round lifecycle, per-round reviewer pools and capacity limits, bulk assignment, per-reviewer progress, and score-sorted aggregate results.
- Assignment creation requires event-scoped reviewer access and rejects speaker/self assignments as explicit conflicts. Duplicate assignment requests are idempotent and reported separately.
- Reviewer endpoints are identity-scoped to assigned proposals. Draft/future rounds remain invisible; blind detail removes people and speaker-section answers; direct organizer submission access by a reviewer returns `403`.
- Reviewers can save partial drafts, submit only valid final scorecards, receive a server-computed weighted score, and recuse with a persisted rationale. Organizer aggregates update from the same review records.
- Two-session local protocol passed draft invisibility → blind assignment → PII denial → required-score rejection → weighted final score `4.33` → organizer `1/1` completion on 2026-08-09.
- The model advisory assessment stores model, original score, reasoning, strengths, and risks; human override preserves the original while setting an effective score, actor, and rationale. A 25-assessment event/day guard protects the free tier. Local stored assessment `82` → human override `76` → transparent read-back passed on 2026-08-09; the production evaluator subsequently exercised the model-backed assessment and retained its evidence under the restricted evaluator bundle.
- Decision delivery requires proposals to enter the matching accept/decline queue, personalizes documented placeholders, sends through verified Resend infrastructure, records provider/error history idempotently, and leaves failed deliveries queued. Final states cannot be bypassed through the status API.
- A successful acceptance atomically records final status and audit history, creates/updates the organization speaker profile, links it to the accepted session, grants an existing user event-scoped speaker access or creates a hashed 30-day invitation, and includes the portal link in the real decision message. Local queue → test-mode delivery → D1 proof passed on 2026-08-09; the production journey verified the same handoff with provider-backed delivery and controlled-inbox receipt.

## Speaker operations implementation evidence

- Accepted speakers receive an event-scoped portal with linked sessions, public profile fields, private logistics, onboarding tasks, published resources, headshots, and requested files. Organizer endpoints remain inaccessible to speaker identities and organizer identities cannot enter speaker-scoped endpoints.
- Headshots and requested content use private R2 objects. File requests enforce type/size policy, keep immutable numbered versions with SHA-256 digests, and expose authorized speaker and organizer downloads only.
- Organizer operations provide readiness totals, filterable event-level speaker statuses, direct CSV/XLSX event import with email deduplication, task review, file approval/needs-changes comments, resource publishing, and bulk task/file assignment. Existing onboarding tasks are automatically assigned during future acceptance handoffs, and every named proposal participant retains their role when acceptance creates the session roster.
- Two-role local protocol passed speaker profile/logistics → task submission → deck/headshot upload → organizer task/file approval → byte-identical authorized downloads on 2026-08-09. The release gate repeats production health and anonymous-auth-boundary checks against the active Worker recorded in the restricted manifest.

## Agenda implementation evidence

- Organizers can configure rooms/tracks, add accepted sessions and non-session blocks, place/move/clear items, and publish only a complete conflict-free schedule. Every mutation is event-scoped, versioned, and audited.
- Placement rejects overlapping use of the same room and independently rejects a shared speaker appearing in overlapping sessions across different rooms. Publication performs a final global scan for both conflict classes.
- The assisted scheduler is a deterministic multi-room greedy algorithm. It accounts for existing occupancy and speaker sets, returns a preview, and applies only compatible placements; it is not mocked or demo data.
- Local protocol passed incomplete publish rejection → combined room/speaker collision → cross-room speaker collision → assisted next-slot placement → publication → clear/move → republish with persisted version increments on 2026-08-09. The release gate verifies the authenticated agenda boundary on both production domains after edge propagation.

## Content management implementation evidence

- Organizer file-request tasks retain instructions and deadlines, assign every accepted speaker, and create a private upload slot for each speaker/session pair. The upload setting is persisted per event and enforced server-side. The speaker portal shows only the signed-in speaker's tasks, sessions, deadlines, status, and upload slots; organizer APIs return `403` to that identity.
- Private R2 uploads enforce visible and server-side PDF, PowerPoint, ZIP, PNG, JPEG, and WebP constraints with a 25 MB ceiling. Re-uploading creates immutable SHA-256-numbered versions; both roles can inspect author/timestamp history, identify the current version, and download any prior version. Per-file comments preserve author and timestamp across roles without generating unwanted comment email.
- The organizer Content workspace exposes all speaker-task pairs with task/incomplete/overdue filters, upload-reflected status, bulk Resend reminders naming every outstanding task/deadline, a central session/speaker file library, version/comment detail, and secure seven-day share links.
- Central editing persists session title/abstract and speaker bio/headshot changes. Every session edit snapshots the prior value with actor and timestamp; restore creates a new revision before applying the selected version. Draft/in-review/approved state is durable, and all public session, speaker, and agenda widget reads require approval while non-session agenda blocks remain publishable.
- Distribution multi-selects uploaded files, supports session/speaker/flat grouping, creates a real ZIP in R2 from latest versions only, enforces a 100 MB generation bound, records ready/failed export history, and returns an authenticated archive download. Workers AI can suggest clarity/tone/length improvements with a 25-per-event daily free-tier guard; suggestions remain reviewable and require an explicit human apply/save.
- The repeatable two-role local protocol passed two tasks × two speakers → scoped speaker upload v1/v2 → cross-role comments → organizer status reflection → two edits/history/restore → speaker bio/headshot → approved-in/unapproved-out public widget gate → latest-only grouped ZIP byte verification → anonymous share-link byte verification on 2026-08-09. Speaker access to content administration returned `403`; the full suite now has 27 passing tests.
- Migration `0007_content_management.sql` executed all ten additive commands successfully against local and production D1. The release gate requires production health `200`, the Content SPA route `200`, protected content administration `401` anonymously, and an invalid/expired share URL `404` against the active Worker.

## Public widget implementation evidence

- Organizers can create and edit five persistent public configurations: sessions, speakers, agenda, personal itinerary, and gallery. Theme, brand color, search, track filtering, selected tracks, and visible fields are stored in D1 and read on every public request, so edits propagate without replacing embed code.
- Anonymous widget reads expose accepted-session and published-agenda read models only. Speaker headshots remain private R2 objects served through an acceptance-scoped public route; organizer endpoints require owner/admin event access.
- Search, track filtering, expandable session details, responsive layouts, durable device-local itinerary choices, and personal ICS export are functional client behavior. JSON, XML, and full-agenda iCal feeds share the same track-restricted server read model.
- Local Worker + D1 protocol created all five types, validated anonymous JSON/XML/iCal payloads, rejected unauthorized administration, and confirmed PATCH propagation on 2026-08-09. Browser protocol rendered the live agenda and confirmed an itinerary addition survived a full reload.
- The embed shell returns `200` with `frame-ancestors *` and no `X-Frame-Options`; normal application routes retain `frame-ancestors 'none'` and `X-Frame-Options: DENY`. The polished Production Readiness event supplies the exact evaluator records and the live five-widget attendee program; the earlier scratch event is archived and unpublished to prevent fixture ambiguity.
- PostHog is configured with the existing US project token, lazy loading, identified-only person profiles, no session recording, and no autocapture. Explicit widget and itinerary events plus route pageviews are enabled. Speaker operations, CRM, content, legal policies, and public interest remain route-split, and XLSX parsing loads only when a spreadsheet is selected.

## Speaker CRM implementation evidence

- Organization-level CRM navigation exposes a persistent cross-event directory with debounced partial search, combinable company/title/tag filters, clearable chips, reusable dynamic or curated segments, CSV export, and validated CSV/XLSX import with column mapping and create/update-by-email behavior.
- Contact profiles retain identity, pronouns, company/title, phone, region, bio, tags, social data, global custom fields, timestamped private notes, sent-email activity, event connections, and session history. Duplicate candidates are identified by name and can be permanently merged into an explicit primary record while preserving linked notes, fields, segments, pipeline history, outreach history, interest submissions, and the operator-selected email.
- The sourcing pipeline persists all eight evaluator stages, drag/select movement, score and rationale, card notes, and actor-attributed stage history. Contacts can be handed into any workspace event without re-entry; the shared speaker profile and explicit event roster connection feed existing speaker operations.
- Bulk outreach personalizes supported merge tags per recipient, sends through the verified Resend domain, and records campaign/recipient provider state for history and analytics. The dashboard derives live contact, event-speaker, returning-speaker, pipeline, company, source, and email metrics from D1 rather than fixtures.
- Optional scope is implemented as required: year-round public speaker-interest forms with speaker-only or speaker-plus-session modes, custom fields, availability windows, future-event association, management/notification metadata, Turnstile validation, automatic contact upsert, and automatic Identified pipeline enrollment. CRM write actions remain owner/admin-only while workspace members have read access.
- The repeatable local D1 smoke protocol passed create/import/search → note/custom field → dynamic segment → pipeline move/note/history → event handoff → public interest-form publication → duplicate merge with duplicate-email selection → live dashboard totals on 2026-08-09. Unit and authorization-boundary coverage brings the project suite to 22 passing tests.
- Migration `0005_speaker_crm.sql` executed all 19 additive commands successfully against local and production D1. Historical Worker `9ec6e52b-ea76-490c-bfa1-007ec873575d` returned production health `200`, protected CRM reads `401` anonymously, public missing forms `404`, and the CRM/public interest SPA routes `200` with the expected frame and content security policy. The final production journey added a durable two-member curated segment and a two-recipient campaign whose provider-confirmed messages both reached `delivered`.

## Airtable and operations evidence

- Airtable-authoritative workspaces use a real audit-driven D1 outbox, Cloudflare Queue consumer, stable external IDs, bounded exponential backoff, retryable conflicts, and pull reconciliation for organizations, events, CRM contacts, and pipeline cards. Client-side Airtable deletions propagate for deletable domains; tenancy-row deletion is intentionally surfaced as a conflict.
- The HMAC-authenticated Airtable webhook is filtered to client table-data add/update/remove changes. Closely spaced pings are durably coalesced, Worker API writes do not echo, a ten-minute cron drains due D1 work without polling Airtable, and the daily payload read keeps the webhook active.
- Owners/admins can see configuration, pending/failed counts, conflicts, resource timestamps, and last sync in the workspace and can run an explicit recovery sync. Identity and authorization remain exclusively in D1.
- Migration `0006_airtable_sync.sql` passed locally and in production. A production QA workspace completed D1 outbox → Cloudflare Queue → Airtable record creation → D1 external-record mapping with zero pending work, zero failures, and zero conflicts on 2026-08-09. Worker `b774d6ad-712a-483c-bfad-00230eff156b` passed health `200`, undiscoverable webhook-path `404`, and anonymous integration `401` probes.
- Third-party error collection has been removed from source, packages, secrets, build configuration, and CSP. Server request, queue, scheduled-task, and integration failures emit structured JSON into enabled Cloudflare Workers Observability; PostHog remains limited to explicit product analytics.
- The event-scoped Organizer Control Room derives eighteen operational categories from live indexed records, deep-links every item, supports severity/owner/status/deadline/track filters, persists audited ownership, and automatically refreshes. Local D1 protocol returned 24 current records, reconciled a blocking-only filter to exactly five records, persisted owner filtering, removed a triaged submission issue without a document reload, denied a reviewer with `403`, and failed closed on an inaccessible event with `404` on 2026-08-09.
- The repeatable `npm run seed:control-room` fixture creates persisted evidence in every category without provider calls. Reconciliation returned all eighteen nonzero category counts (45 total issues: 20 blocking, 24 warning, 1 informational) and a bounded 100-record window on 2026-08-09.
- Browser verification at 1512×808 and 390×844 found no horizontal overflow. The mobile layout includes an accessible event-workspace selector; URL-addressed severity filtering rendered five blocking rows and no nonblocking work. Production verification later exercised recovery from integration failure and reconciled the polished buyer event to zero visible blockers.

- Event creation now requires an exact preview and can materialize the four maintained starter programs, a versioned organization template, or a same-organization event. Local protocol created conference and organization-template events with translated deadlines and durable provenance; their configuration reconciled to 1 form, 4 fields, 1 round, 2 criteria, 3 tasks, and 3 communication templates while submissions, files, sent messages, calendar records, and external IDs each reconciled to exactly zero. A forced SQL failure retained a failed operation and removed the partial event. Reviewer access returned `403`; a valid organizer attempting to copy another tenant's event received the same scoped `404` as an absent source.

- Browser verification exercised starter preview, explicit exclusion disclosure, confirmation, event creation, saved-template creation, and reuse in a new event. It surfaced and led to correction of an asynchronous form-reset/focus defect. Selected-timezone conversion has automated London/New York and DST-gap coverage. The final production journey created its buyer event from a maintained starter, and the cross-feature desktop/mobile release gate passed.

- The organizer-wide command palette searches all twelve required entity types with bounded indexed queries and deterministic exact/prefix/word/contains/context/fuzzy ranking. Effective event roles are resolved before each entity query: reviewer protocol returned only one assigned proposal and one published session, speaker protocol returned only the signed-in speaker's own profile/proposal/session, and an inaccessible event scope failed closed. Recent destinations persist in D1, cap at twenty per user, and are independently reauthorized against their source record rather than trusting an earlier search window.
- Local protocol searched a 1,200-submission fixture while returning a bounded 40-result response; a deliberately misspelled `relible` query found `Reliable Systems` through the bounded fuzzy rank. Browser verification passed Command+K focus, grouped results, arrow/Enter navigation, reviewer-invitation focus and event preselection, CRM speaker-handoff preselection, and reminder-compose routing without executing a mutation. Structured output contains query length but no query text; explicit PostHog events likewise omit search text and personal data. The final desktop/mobile production gate reverifies the palette and authorization boundaries; any reported latency is sourced from the fully qualified production measurement in the restricted release manifest.

- The durable notification center now covers all required domain types across twelve categories, paginates and filters on indexed recipient data, persists individual/all read state, coalesces repeated work, exposes role-scoped links, and audits event/organization preference changes. Channel preferences independently support in-app and explicit opt-in email; email uses deterministic Communications/Queue messages, not a fake delivery flag. Daily cleanup archives at 180 days and deletes after 30 more days.
- `npm run seed:notifications` restored 22 persisted examples spanning every required type. Browser protocol reconciled 22 unread rows and five blocking rows, verified severity filtering while the global bell remained 22, changed and restored an event preference, marked blocking work read, and confirmed the remaining 17 unread state after a new page load. Two real speaker-profile mutations produced exactly two authorized manager rows whose occurrence counts advanced from one to two rather than duplicating records. Preference changes produced two audit rows. Production desktop/mobile tests, cross-recipient isolation, durable read state, and controlled-inbox opt-in email evidence are preserved in the final bundle.

## Production hardening evidence

- The repository Playwright gate exercises public routes, the versioned developer API, and authenticated operational surfaces in desktop Chromium and a mobile Chromium device profile. It runs WCAG 2.1 A/AA assertions, checks horizontal overflow, verifies command-palette and notification focus behavior, confirms explicit judge-first controls, covers organizer, reviewer, and speaker workspaces, and includes negative cross-event API probes. Authentication is accepted only through ignored storage-state paths; no session or token value is committed. The exact production release passed 30/30 desktop/mobile cases.
- Additive production migrations `0001`–`0025` are applied, and the production migration command reports no pending work. A fresh exact replay completes with an empty `PRAGMA foreign_key_check`; `0019`–`0021` add speaker-file, calendar-cancellation, review-pool/capacity, and event-speaker state; `0022` adds category-based routing; `0023`–`0024` add the developer platform and rotating OAuth refresh tokens; and `0025` adds organization-managed safe embed domains. The exact active source commit and Worker version are recorded in the restricted production manifest and independently returned by `/api/health`; the final verifier requires both values to match. Normal application pages send HSTS with subdomains, a restrictive CSP, no-sniff, permissions policy, strict-origin referrer policy, and frame denial. Embed pages retain the intentionally narrow framing exception.
- Magic-link and invitation sessions are random-token, SHA-256-indexed, revocable, expiring, `HttpOnly`, `Secure` in production, and `SameSite=Lax`. Anonymous authentication, CFP, and interest-form writes are protected by Cloudflare Turnstile. Server-side organization/event membership checks return nondisclosing `404` for absent scope and `403` for insufficient roles.
- Speaker resource HTML now passes through matching client/server sanitizer policies. Only built-in reference providers or exact HTTPS domains approved by an organization admin survive, and every frame is sandboxed, lazy-loaded, no-referrer, and responsive. Executable markup, event handlers, unsafe URLs, forms, popups, top-level navigation, and unapproved frames are removed; the preview explains removals before publishing.
- `npm audit --audit-level=high` reports zero vulnerabilities. Typecheck, the complete unit/integration suite, and the production build are required release gates; exact counts are frozen in the production manifest only after the final run. Browser smoke coverage passes navigation, text/select input, upload, drag, click, and screenshot operations; the complete dry run validates all 7 areas, 20 scenarios, and 96 rubrics.
- A production axe-core run across marketing, login, Privacy, Terms, authenticated organizer dashboard, and the populated event workspace reports zero WCAG 2 A/AA/2.1 AA violations at 1440×1000 and 390×844. Every measured layout has no horizontal overflow. Timing claims are published only from the final route-, environment-, region-, device-, sample-, and method-qualified measurement.
- The complete production Playwright gate passed 30/30 across desktop and mobile Chromium, including public marketing/help/developer routes, scoped developer API/query/MCP access, authenticated Control Room, Communications, submission workspace, calendar, explicit workflow actions, all three persona workspaces, keyboard palette/notifications, accessibility, overflow, and negative cross-event authorization probes. Stale lazy chunks return uncached `404` responses and the client performs one bounded recovery reload, preventing a post-deployment asset rollover from producing a blank route.
- A repeatable, production-confirmed stateful verification created the isolated `ProgramLoom Production Readiness` event from the conference starter, published its real CFP, handed off a CRM contact, created invite-based speaker/reviewer access, and sent controlled communications through the verified domain. The event mutation drained D1 → Queue → Airtable from one pending record to zero with no failures or conflicts. Controlled inbox receipt was confirmed by the account owner.
- The Resend webhook is enabled for every lifecycle event and signs callbacks into the Communications center. Live verification exposed and fixed a malformed provider-status audit insert, then exposed and fixed an out-of-order `email.sent` downgrade race. The final controlled message persisted distinct `email.sent` and `email.delivered` events with final state `delivered`; the affected disposable record was reconciled with a durable audit entry. Regression coverage now asserts system audit correlation and terminal-state monotonicity.
- The production readiness journey accepted a real proposal, created portal/task/file/session records, uploaded and approved a fictional speaker headshot plus a five-page PDF deck in R2, scheduled and published the session, and delivered the initial invitation and material room/time update. A disposable accepted session then proved stable-UID sequences `0 REQUEST` → `1 CANCEL` → explicit-reschedule `2 REQUEST` → final `3 CANCEL`; ordinary reschedule returned `409`, reviewer/speaker cancellation returned `403`, and the final public widget excluded the item. Control Room and Airtable each reconciled to zero outstanding work, while the audit, communication, notification, external-ID, and calendar revision histories remained durable. Evidence artifacts are retained under `docs/evidence/production-journey/` and `output/pdf/`.
- Production search isolation returned no results for the blind reviewer’s `Priya Raman` query, while their assigned proposal and its already-public agenda session remained discoverable by title. The speaker identity could find only her own profile, proposal, session, and addressed calendar communications. Exact downloaded invitation bytes are retained for both main-session revisions and all four disposable lifecycle revisions; each has a stable per-session UID, monotonic sequence, matching `REQUEST`/`CANCEL` method and status, folded organizer/attendee fields, UTC timestamps, and a delivered provider-backed communication.
- Public-widget production depth includes multiple approved sessions and speakers, a multi-day accessible agenda grid, visible per-session itinerary actions, speaker-centered profile cards, event-timezone labeling, and all five exact anonymous widget URLs. The focused production run verified 34/34 automated criteria, including two-session selection, reload persistence, ICS export, removal, all five anonymous surfaces, and organizer embed generation. Cards expose speaker title/company, itinerary room/format/full speaker metadata, profile session date/time/room, and an explicit Close details control.
- Evaluator spend is governed by the current owner-approved ceiling and a fail-closed request guard. Exact final spend, remaining allowance, report, and request ledger are retained only in the restricted evidence bundle and are regenerated after the final product freeze; no stale interim amount is presented as final.
- A real production itinerary interaction emitted the privacy-bounded custom event to the configured US PostHog ingestion endpoint and received HTTP `200`; query contents and personal data are not included. Airtable recovery was also exercised after evaluator-created agenda churn exposed repeated conflict-history uniqueness. The processor now compacts an older resolved snapshot before resolving a repeated conflict. The replay completed with pending `0`, failed `0`, open conflicts `0`, 33 external mappings, and successful current sync state for all four reconciled resources.
- Gmail and Apple Calendar verified creation, in-place update, and cancellation behavior. Outlook was explicitly waived by the account owner because no account was available; no Outlook evidence is claimed.

## Evaluator identity and isolation evidence

- One evaluator event defines the intended identities and lifecycle state. Revocable, expiring production sessions exist for Jordan Alvarez, Priya Raman, and Sam Whitfield in ignored evaluator auth-state files; tokens are hashed in D1, never printed, and not committed.
- Jordan owns the Airtable-authoritative `DevFlow Programs` workspace and its polished `ProgramLoom Production Readiness` event. Its exact CFP examples, proposal variants, weighted review configuration, speaker work, schedule, and public widgets form one coherent record graph. The earlier evaluator scratchpad is archived and its CFP is unpublished.
- The baseline workspace/event mutation completed D1 → durable outbox → Cloudflare Queue → Airtable with two external records, zero pending jobs, zero failures, and zero conflicts. Explicit reconciliation succeeded across organizations, events, CRM contacts, and pipeline cards.
- Production isolation probes return event access `200` for Jordan, `401` anonymously, and nondisclosing `404` outside permitted event scope. Priya and Sam have only explicit Production Readiness access, so speaker, reviewer, and organizer scenarios preserve the same event and records without broadening either persona's scope.
